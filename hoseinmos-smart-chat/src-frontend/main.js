import "./style.css"; document.addEventListener("DOMContentLoaded", function() { const widget = document.getElementById("hmsc-widget"); const bubble = document.getElementById("hmsc-bubble"); const window = document.getElementById("hmsc-window"); const close = document.getElementById("hmsc-close"); const form = document.getElementById("hmsc-form"); const input = document.getElementById("hmsc-input"); const messages = document.getElementById("hmsc-messages"); const send = document.getElementById("hmsc-send"); let isOpen = false; function toggleChat() { isOpen = !isOpen; window.style.display = isOpen ? "block" : "none"; if (isOpen) { input.focus(); } } function addMessage(text, isUser = false) { const message = document.createElement("div"); message.className = "hmsc-message " + (isUser ? "hmsc-message-user" : "hmsc-message-bot"); const content = document.createElement("div"); content.className = "hmsc-message-content"; content.textContent = text; message.appendChild(content); messages.appendChild(message); messages.scrollTop = messages.scrollHeight; } function sendMessage() { const message = input.value.trim(); if (!message) return; addMessage(message, true); input.value = ""; send.disabled = true; send.innerHTML = "<span>Sending...</span>"; fetch(HMSC_CONFIG.rest_url + "ask", { method: "POST", headers: { "Content-Type": "application/json", "X-WP-Nonce": HMSC_CONFIG.nonce }, body: JSON.stringify({ message: message }) }) .then(response => response.json()) .then(data => { addMessage(data.answer); }) .catch(error => { addMessage("Sorry, something went wrong. Please try again."); }) .finally(() => { send.disabled = false; send.innerHTML = "<svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M2.01 21L23 12L2.01 3L2 10L17 12L2 14L2.01 21Z\" fill=\"currentColor\"/></svg>"; }); } bubble.addEventListener("click", toggleChat); close.addEventListener("click", toggleChat); form.addEventListener("submit", function(e) { e.preventDefault(); sendMessage(); }); });
